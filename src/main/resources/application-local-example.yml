spring:
  datasource:
    url: url
    username: username
    password: password

photo:
  upload:
    base-dir: url

cors:
  allow-url: url

cloud:
  aws:
    region:
      static: ap-northeast-2
    s3:
      bucket: name
    cdn:
      base-url: url

kakao:
  api:
    rest-api-key:
    login-oauth:
      redirect-url: http://localhost:8080/api/v1/login/kakao
